<!DOCTYPE html>
<html lang="ru">
<head>
    <title>{{ title }}</title>
    <meta name="description" content="{{ description }}">
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <link href="{{ base_url }}assets/css/bootstrap.min.css" rel="stylesheet">
    <link href="{{ base_url }}assets/css/main.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <nav  class="navbar navbar-inverse col-xs-12" role="navigation">
        <div class="container">
            <a href="#" class="open_nav_btn">Меню</a>
        <ul class="font_nav list-inline nav navbar-nav navbar-left">
            <li><a href="{{ base_url }}catalog/showProduct">Каталог</a></li>
            <li>
                <a href="{{ base_url }}cart">
                    <div class="cart-div">
                        <span class="glyphicon glyphicon-shopping-cart"></span>
                        <div class="cart-total-items-div">
                            <span id="total-cart-items"><div class="total-cart-items-id">{{ total_items }}</div></span>
                        </div>
                        <div class="clear"></div>
                    </div>

                </a>
            </li>
        </ul>
        <ul class="list-inline nav navbar-nav navbar-right">
            {% for pages in pages %}
                <li><a href="{{ base_url }}pages/showpage/{{ pages.name }}">{{ pages.title }}</a></li>
            {% endfor %}
        </ul>
        </div>
    </nav>
    <div class="container">
    <div class="row">
        <div class="col-xs-12 col-md-12 head">
            <div class="hidden-xs col-sm-4 col-md-4 head_text_div">
                <div class="head_text">
                    <p>Магазин ...</p>
                    <p>Все по самым низким ценам</p>
                    <p>Метро Метро, Адрес Адрес</p>
                    <p>11:00 - 23:00</p>
                    <p>
                        <a href="https://www.instagram.com/" target="_blank">
                            <div class="insta_div">
                            <img src="{{ base_url }}assets/images/work/insta.png" class="insta_img img-responsive">
                            </div>
                        </a>
                    </p>
                </div>
            </div>
            <div class="col-xs-5 col-sm-4 col-md-4">
                <div class="cont_logo_img">
                    <a href="{{ base_url }}main">
                        <img src="{{ base_url }}assets/images/work/logo.png" alt="logo" class="logo_img img-responsive">
                    </a>
                </div>
            </div>
            <div class="col-xs-7 col-sm-4 col-md-4 text-right menu1">
                <ul class="list-inline">
                    {% if is_logged_in is not empty %}
                        <li><a href="{{ base_url }}account/accountorders">Личный кабинет</a></li>
                    {% else %}
                        <li><a href="{{ base_url }}account">Личный кабинет</a></li>
                    {% endif %}
                    <li><a href="{{ base_url }}registration/register">Регистрация</a></li>
                </ul>
                <form name="search" method="post" id="searchform" action="#">
                    <input type="text" class="search_form form-control" name='param' placeholder="Поиск...">
                    <button type="button" id='searchprod' class="search_button btn btn-default search-btn"><span class="glyphicon glyphicon-search"></span></button>
                </form>
            </div>
        </div>
    </div>
    </div>

</head>